---
import { pages } from "../../config/site";
import { getSectionEntries } from "../../lib/content";
import BaseListing from "../../layouts/BaseListing.astro";

const entries = await getSectionEntries("writing");
---

<BaseListing collection="writing" entries={entries} {...pages.writing} />
